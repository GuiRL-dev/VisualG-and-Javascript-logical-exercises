var macas = parseInt(prompt("Digite o numero de macas: "));
var valorMacas = 1.30;


if (macas >= 12) {
  valorMacas = 1;

  alert("Valor total: " + macas * valorMacas);

}
else {
  alert("Valor total: " + macas * valorMacas);
}